# Tested on Python 3.12.10 — Windows
# Run: pip install -r requirements.txt

fastapi==0.137.2
starlette==1.3.1          # FastAPI's ASGI core; pinned because tests import starlette.websockets directly
uvicorn[standard]==0.49.0
pydantic==2.13.4
websockets==16.0
plyer==2.1.0
python-dotenv==1.2.2

# Dev / test
ruff==0.15.18
pytest==9.1.0
pytest-asyncio==1.4.0
httpx==0.28.1
mcp==1.28.0              # Agent-Relay plugin: stdio MCP server (server/relay_mcp.py)
