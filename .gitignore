# Runtime data — never committed (DB, secrets, local_config.json, WAL/SHM sidecars)
/data/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
extension/node_modules/
extension/dist/

# Test & tool caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
/tmp/

# Agent working artifacts & local dev tooling (not part of the product)
/.cc/
/.codex/
/.helpers/

# Env / secrets
.env
*.secret

# Internal design/planning history — kept locally, not part of the public repo
/agent-slack/
/docs/superpowers/
/docs/probe-results.md
